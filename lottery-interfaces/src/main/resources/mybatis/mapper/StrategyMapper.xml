<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="lottery.infrastructure.dao.IStrategyDao">
    <select id="queryStrategy" parameterType="java.lang.Long" resultType="lottery.infrastructure.po.Strategy">
        SELECT id,
               strategy_id,
               strategy_desc,
               strategy_mode,
               grant_type,
               grant_date,
               ext_info,
               create_time,
               update_time
        FROM strategy
        WHERE strategy_id = #{strategyId}
    </select>
</mapper>
